main = f(A, B, N)
   for i,j in [1..N],[1..N]
   returns array of for initial
      s = 0.0
      k = 1
   while (k <= N)
   repeat
      s = old s + A[i, old k] * B[old k, j]
      k = old k + 1
   returns value of s
